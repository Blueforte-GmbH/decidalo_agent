---
description: Show the active CV standardization rules — which rules exist, what fields they affect, and whether any are incomplete.
---

Show the active CV standardization rules from the `rules/` folder.

Use the `$list-rules` skill: run its script, then summarize for the user which
rules are active, what each one does, and flag any incomplete rule.

Optional focus from the user (e.g. a specific rule or field): $ARGUMENTS
